settings:
  infer_schema: True
  infer_fields: True
  row_limit: 2000
  checks:
    - structure
    - schema
    - blank-header
    - duplicate-row
    - missing-value
  skip_checks:
    - unique-constraint
